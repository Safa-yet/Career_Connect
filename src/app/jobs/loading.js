import Loading from '@/Components/Common Sec/Loading';
import React from 'react';

const loading = () => {
    return (
        <div>
            <Loading></Loading>
        </div>
    );
};

export default loading;